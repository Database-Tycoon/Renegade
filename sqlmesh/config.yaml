gateways:
  local:
    connection:
      type: duckdb
      database: ../data/nycdata.db
      extensions: ['httpfs']

default_gateway: local

model_defaults:
  dialect: duckdb
  start: 2020-01-01 # TODO update
